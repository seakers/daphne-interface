// initial state

const state = {
    scoreInfo: []
};

// getters
const getters = {
};

// actions
const actions = {
};

// mutations
const mutations = {
    setScoreInfo(state, scoreInfo) {
        state.scoreInfo = scoreInfo;
    }
};

export default {
    state,
    getters,
    actions,
    mutations
}
