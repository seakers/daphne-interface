// initial state
const state = {
    launchCosts: []
};

// getters
const getters = {
};

// actions
const actions = {
};

// mutations
const mutations = {
    setLaunchCosts(state, launchCosts) {
        state.launchCosts = launchCosts;
    },
};

export default {
    state,
    getters,
    actions,
    mutations
}
