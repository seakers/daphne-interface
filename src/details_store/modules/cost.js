// initial state
const state = {
    budgets: []
};

// getters
const getters = {
};

// actions
const actions = {
};

// mutations
const mutations = {
    setBudgets(state, budgets) {
        state.budgets = budgets;
    },
};

export default {
    state,
    getters,
    actions,
    mutations
}
